export interface ThemeToggleProps {
  className?: string;
}

/**
 * Theme toggle (abolished in favor of pure light medical theme).
 */
export function ThemeToggle(_props: ThemeToggleProps) {
  return null;
}
